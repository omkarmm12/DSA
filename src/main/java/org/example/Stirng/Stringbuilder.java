package org.example.Stirng;

public class Stringbuilder {                 // creates modifiable strings (mutable strings). same as StringBuffer except it is non-synchronized

    public static void main(String[] args){

        Stringbuilder sb=new Stringbuilder();   // same methods as StringBuffer
    }
}
